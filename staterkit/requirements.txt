Flask>=2.0.0
Flask-SQLAlchemy>=3.0.0
Flask-Login>=0.6.0
Flask-Assets>=2.0
Flask-WTF>=1.0.0
WTForms>=3.0.0
Flask-Caching>=2.0.0
libsass>=0.21.0
openpyxl>=3.1.0
reportlab>=4.0.0
Flask-Migrate>=4.0.0
flask-jwt-extended>=4.5.0
flask-limiter>=3.0.0
psycopg2-binary>=2.9.0
elasticsearch>=8.0.0
flasgger>=0.9.7
pyotp>=2.9.0
qrcode>=7.4
flask-socketio>=5.3.0
python-dotenv>=1.0.0
pytest>=8.0
pytest-flask>=1.3
APScheduler>=3.10

# Serving. Required, not optional: dseclab.service execs venv/bin/gunicorn and
# gunicorn.conf.py sets worker_class = "eventlet". Installing only the rest of
# this file yields a venv whose service cannot start (systemd 203/EXEC).
# Upper bound is load-bearing: gunicorn removed the eventlet worker in a
# later release, and gunicorn.conf.py sets worker_class = "eventlet", so an
# unpinned install boots into
#   ImportError: Entry point ('gunicorn.workers', 'eventlet') not found
# eventlet is also deprecated upstream — moving to gevent is the real fix,
# and this pin is what holds the line until then.
gunicorn>=21.2,<23
eventlet>=0.35
